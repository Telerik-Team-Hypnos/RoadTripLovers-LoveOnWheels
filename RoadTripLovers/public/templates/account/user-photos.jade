.col-md-6
	h3(ng-if="photos.collection.length===0") No images uploaded!
.col-md-4
	.list-group(ng-if="isMyProfile")
		h3.list-group-item.active.text-center Upload a new photo
		.list-group-item
			form(ng-submit="photos.uploadPhoto()" enctype='multipart/form-data')
				.form-group
					label Select a photo
					input(type='file' name='new-photo' ng-model='photos.newPhoto')
				button.btn.btn-primary Upload